# values.yaml – personalize aqui
directus:
  env:
    KEY: "f3a9c6b1e2d4a8f7c9b0d1e3f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4"          # gera com: openssl rand -hex 32
    SECRET: "outro-segredo-forte-aqui"              # outro random
    ADMIN_EMAIL: "admin@directus.com"
    ADMIN_PASSWORD: "senha_teste"
    
    DB_CLIENT: "pg"                                 # ou "mysql" se preferir
    DB_HOST: "postgres-directus"                    # vamos usar Postgres abaixo
    DB_PORT: "5432"
    DB_DATABASE: "directus"
    DB_USER: "directus"
    DB_PASSWORD: "directus123"

  ingress:
    enabled: false
    className: "traefik"
    hosts:
      - host: directus.seu-ip.nip.io                # ← troque pelo seu IP ou domínio
        paths:
          - path: /
            pathType: Prefix
    tls: []                                        # vamos deixar HTTPS depois com Cert-Manager

postgresql:
  enabled: true                                    # instala Postgres junto
  auth:
    postgresPassword: "postgres123"
    database: "directus"
    username: "directus"
    password: "directus123"

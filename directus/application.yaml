apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: directus
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/mpletti/directus.git   # ← seu repo
    path: directus
    targetRevision: main
    helm:
